class PriorityQueueMap:
    def __init__(self):
        # Use a tree or a heap to keep in ordered?

    # O(1)
    def insert(self, vertex, edge, cost):
        # if cost < existing cost, then insert
        # Compare and replace

    # O(N)
    def shift(self):
        # Need to rafactor to make it O(1) or log(n)
        # Takes the smallest cost item
